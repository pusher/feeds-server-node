{
  "name": "pusher-feeds-server",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "install-pkg": "if type yarn &> /dev/null; then yarn; else npm i; fi",
    "start": "NODE_TLS_REJECT_UNAUTHORIZED=0 nodemon auth.js --exec babel-node --presets es2015 stage-2 --plugins transform-class-properties transform-flow-strip-types"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "express-session": "^1.15.3"
  }
}
