{
  "name": "pusher-feeds-server",
  "version": "0.1.0",
  "main": "./build/index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d build --source-maps",
    "test": "echo Testing...\n",
    "install-pkg": "if type yarn &> /dev/null; then yarn; else npm i; fi",
    "example": "cd examples && npm start",
    "postinstall": "cd examples && npm run install-pkg"
  },
  "dependencies": {
    "jsonwebtoken": "^7.4.1",
    "pusher-platform": "https://github.com/pusher/pusher-platform-nodejs.git#master"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "flow-bin": "^0.47.0"
  }
}
